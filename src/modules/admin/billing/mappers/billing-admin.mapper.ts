export const billingAdminMapper = {
  normalizeSubscription(data: any): any {
    return data;
  },
  normalizePayment(data: any): any {
    return data;
  },
  normalizeInvoice(data: any): any {
    return data;
  }
};
